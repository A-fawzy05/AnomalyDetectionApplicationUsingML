apiVersion: v1
kind: Service
metadata:
  name: django
  namespace: p2p
  labels:
    app: django
spec:
  type: ClusterIP
  selector:
    app: django
  ports:
    - name: http
      port: 8000
      targetPort: 8000
